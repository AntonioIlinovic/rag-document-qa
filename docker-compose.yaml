services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app  # For development - enables hot reload
      - ./app_data:/app/app_data  # Mount root-level app_data
    restart: unless-stopped