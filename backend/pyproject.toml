[project]
name = "backend"
version = "0.1.0"
description = "Backend for RAG document QA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "uvicorn>=0.41.0",
]
