[project]
name = "backend"
version = "0.1.0"
description = "Backend for RAG document QA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.135.1",
    "httpx>=0.28.1",
    "pymupdf>=1.27.2",
    "pytest>=9.0.2",
    "pytest-asyncio>=1.3.0",
    "uvicorn>=0.41.0",
]
